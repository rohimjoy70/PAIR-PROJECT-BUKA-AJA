<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
            margin: 20px;
        }

        nav {
            margin-bottom: 20px;
        }

        h1, h3 {
            color: #007bff;
        }

        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s ease-in-out;
        }

        .card:hover {
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .card-title {
            color: #343a40;
        }

        .card-text, .card-title {
            margin-bottom: 10px;
        }

        .btn-primary, .btn-danger {
            margin-top: 10px;
        }

        footer {
            margin-top: 20px;
            padding: 10px 0;
            background-color: #343a40;
            color: #fff;
            text-align: center;
        }
        
    </style>
    <title>Otw Buka List</title>
</head>
<body>
    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/store">Store</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/product">All Product</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">Log Out</a>
                </li>
            </ul>
        </div>
    </nav>

    <hr />

    <!-- JUDUL -->
    <a href="/product/add/">
        <button type="button" class="btn btn-primary">Add Product</button>
    <h1 class="text-center">Otw Buka</h1>
    <h3 class="text-center"><%=store.code%></h3>

    <hr />

    <!-- TABLE -->
    <div class="row justify-content-center">
        <% if (data === {}) { %>
            <p>No products available</p>
        <% } else { %>
            <% data.forEach(el => { %>
                <div class="card m-2" style="width: 18rem;">
                    <img class="card-img-top" src="<%= el.imageURL %>" alt="Card image cap" />
                    <div class="card-body">
                        <h5 class="card-title"><%= el.name %></h5>
                        <p class="card-text"><%= el.description %></p>
                        <h6 class="card-title">STOCK: <%= el.stock %></h6>
                        <h6 class="card-title">Price: <%= money.rupiah(el.price) %></h6>
                        <a href="/product/buy/<%= el.id %>" class="btn btn-primary">Buy</a>
                        <a href="/product/delete/<%= el.id %>" class="btn btn-danger">Delete</a>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </div>
    

    <footer>
        &copy; 2024 Otw Buka List. All Rights Reserved.
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-rbsm/5P1lI6g64xZ6wNfF5Px4FdmYF5FZiO/HP8Zjfu6eC5Y+gnazpQgSzUpJlKZ8F" crossorigin="anonymous"></script>
</body>
</html>
